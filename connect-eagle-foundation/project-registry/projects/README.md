# Registered projects

One `<project-slug>.yml` file per participating repository. This directory starts empty intentionally: examples and dogfood records are not memberships.

To register, use `connect-eagle submit --registry OWNER/project-registry --push` after this repository is published, or open a PR adding a schema-v1 record. Repository ownership stays with the original maintainer. A registry maintainer reviews the request before merging.
